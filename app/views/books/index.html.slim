.container-fluid
  .row
  .col-md-6
    h1 Todos los libros
    table.table
      tr
        th Titulo
        th Autores
        th ISBN
        th
        th Copias disponibles
        th
      - @books.each do |book|
        tr.info
          td= book.title
          td= book.author
          td= book.isbn
          td= link_to 'Show', book_path(book)
          td
          td
          - book.copies.each do |copy|
            tr
              td
              td
              td
              - avatar = copy.user.avatar.present? ? copy.user.avatar : 'default_avatar.png'
              td= image_tag(avatar, class: 'l-profile-image')
              td= copy.user.email
              td= link_to 'Pedir prestado', edit_book_path(copy)